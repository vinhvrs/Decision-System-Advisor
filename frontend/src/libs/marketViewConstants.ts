/** Volume movers (gainers / losers) list length on trading / beginner extras. */
export const MARKET_MOVERS_LIST_LIMIT = 3;

/** Matches `company_profile.sector` substring (case-insensitive), e.g. Yahoo “Technology”. */
export const HEATMAP_SECTOR_TECHNOLOGY = "Technology";
